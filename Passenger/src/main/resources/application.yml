server:
  port: 8083


spring:

  application:
    name: "passenger"

  datasource:
    url: jdbc:mysql://localhost:3306/passengerdb
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
